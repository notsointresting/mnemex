--- a/src/payments.py
+++ b/src/payments.py
@@ def process_payment(order_id, amount, idempotency_key):
-    if not idempotency_key:
-        raise ValueError("idempotency key is required")
-    if _already_charged(idempotency_key):
-        return {"status": "duplicate", "order_id": order_id}
+    _ensure_single_charge(idempotency_key, order_id)
     _write_ledger(order_id, amount, idempotency_key)
     return {"status": "charged", "order_id": order_id, "amount": amount}
+
+def _ensure_single_charge(idempotency_key, order_id):
+    if not idempotency_key:
+        raise ValueError("idempotency key is required")
+    if _already_charged(idempotency_key):
+        raise DuplicateCharge(order_id)
